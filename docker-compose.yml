# agentflow -- LibreChat + agent-skills MCP + an OpenAI-compatible LLM (vLLM)
#
# Every service is a pinned image; every config a mounted file; every byte of
# durable state a named volume (see docs/OPERATIONS.md). Nothing is baked
# into an image build, so this maps 1:1 onto Deployments/PVCs later.
#
# mcp-agent-skills deliberately has no published port and is on the backend
# network only -- fastmcp's HTTP transport has no authentication of any
# kind. Reachability from `api` over the internal network is the only access
# control it has; do not add a `ports:` entry to it.

networks:
  frontend:
  backend:

volumes:
  mongodb_data:
  pgvector_data:
  kb_data:
  meili_data:
  librechat_uploads:
  librechat_images:
  librechat_logs:

services:
  api:
    image: ghcr.io/danny-avila/librechat:${LIBRECHAT_IMAGE_TAG:?set LIBRECHAT_IMAGE_TAG in .env}
    restart: unless-stopped
    ports:
      # Bound to all interfaces (0.0.0.0) so clients on the LAN can reach the
      # UI directly. The repo's upstream default is 127.0.0.1-only -- if this
      # box ever gets a public interface, or you want TLS, re-bind to
      # 127.0.0.1 and put a reverse proxy in front instead.
      - "${PORT:-3080}:${PORT:-3080}"
    # Pin the model host (VLLM_BASE_URL's host) to a fixed IP for container
    # DNS, for the services that call the model. Defaults are harmless
    # placeholders -- set MODEL_HOSTNAME / MODEL_HOST_IP in .env to pin a real
    # host (needed when it doesn't resolve from this box the way you want).
    extra_hosts:
      - "${MODEL_HOSTNAME:-your-model-host}:${MODEL_HOST_IP:-192.0.2.1}"
    depends_on:
      mongodb:
        condition: service_healthy
      meilisearch:
        condition: service_healthy
      rag_api:
        condition: service_started
      mcp-agent-skills:
        condition: service_healthy
    env_file: .env
    environment:
      HOST: 0.0.0.0
      MONGO_URI: mongodb://${MONGO_APP_USERNAME}:${MONGO_APP_PASSWORD}@mongodb:27017/LibreChat?authSource=admin
      MEILI_HOST: http://meilisearch:7700
      RAG_API_URL: http://rag_api:8000
      ADMIN_PANEL_URL: http://localhost:${ADMIN_PANEL_PORT:-3000}
    volumes:
      # Generated (gitignored) from config/librechat.yaml.example by
      # scripts/render-librechat-config.sh -- `make up` runs this before
      # every `docker compose up`, so it always exists by the time this
      # mount is needed. See AGENTS.md "Configuration is generated".
      - type: bind
        source: ./config/librechat.yaml
        target: /app/librechat.yaml
        read_only: true
      - librechat_uploads:/app/uploads
      - librechat_images:/app/client/public/images
      - librechat_logs:/app/api/logs
    networks: [frontend, backend]
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    healthcheck:
      # The LibreChat image ships wget, not curl -- confirmed by exec'ing in
      # and checking, after curl-based healthchecks failed with
      # "executable file not found in $PATH".
      test: ["CMD", "wget", "--quiet", "--spider", "http://127.0.0.1:${PORT:-3080}/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    mem_limit: 2g

  # Standalone management UI (users, groups, roles, config overrides) --
  # github.com/ClickHouse/librechat-admin-panel, LibreChat's own bundled
  # service. Admin access is checked by `api` itself (role: 'ADMIN' in
  # Mongo, or the access:admin grant), so this container can't grant
  # itself anything -- see docs/CONFIGURATION.md.
  admin-panel:
    image: registry.librechat.ai/clickhouse/librechat-admin-panel:latest
    restart: unless-stopped
    ports:
      # Bound to all interfaces (0.0.0.0) so the panel is reachable over the
      # network, not just from this box -- same shape as `api` above.
      - "0.0.0.0:${ADMIN_PANEL_PORT:-3000}:3000"
    depends_on:
      api:
        condition: service_healthy
    environment:
      PORT: "3000"
      SESSION_SECRET: ${ADMIN_PANEL_SESSION_SECRET:?set ADMIN_PANEL_SESSION_SECRET in .env}
      API_SERVER_URL: http://api:${PORT:-3080}
      VITE_API_BASE_URL: http://localhost:${PORT:-3080}
      SESSION_COOKIE_SECURE: "false"
    networks: [backend]
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    mem_limit: 512m

  mongodb:
    image: mongo:8.0.20
    restart: unless-stopped
    command: ["--auth"]
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
      # Read by mongo-init/init-librechat-user.sh, not by mongod itself --
      # the official mongo image runs every /docker-entrypoint-initdb.d/
      # script with the container's own env available, exactly once, only
      # when mongodb_data is empty.
      MONGO_APP_USERNAME: ${MONGO_APP_USERNAME}
      MONGO_APP_PASSWORD: ${MONGO_APP_PASSWORD}
    volumes:
      - mongodb_data:/data/db
      - ./mongo-init/init-librechat-user.sh:/docker-entrypoint-initdb.d/init-librechat-user.sh:ro
    networks: [backend]
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping')"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s
    mem_limit: 1g

  meilisearch:
    image: getmeili/meilisearch:v1.35.1
    restart: unless-stopped
    environment:
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY:?set MEILI_MASTER_KEY in .env}
      MEILI_NO_ANALYTICS: "true"
    volumes:
      - meili_data:/meili_data
    networks: [backend]
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--spider", "http://127.0.0.1:7700/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s
    mem_limit: 512m

  vectordb:
    image: pgvector/pgvector:0.8.0-pg15-trixie
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mydatabase}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgvector_data:/var/lib/postgresql/data
    networks: [backend]
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB:-mydatabase}"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s
    mem_limit: 1g

  rag_api:
    image: ghcr.io/danny-avila/librechat-rag-api-dev-lite:${RAG_API_IMAGE_TAG:?set RAG_API_IMAGE_TAG in .env}
    restart: unless-stopped
    # Same model-host pin as `api` -- rag_api also calls VLLM_BASE_URL (for
    # embeddings). See the api.extra_hosts comment above.
    extra_hosts:
      - "${MODEL_HOSTNAME:-your-model-host}:${MODEL_HOST_IP:-192.0.2.1}"
    depends_on:
      vectordb:
        condition: service_healthy
    environment:
      DB_HOST: vectordb
      POSTGRES_DB: ${POSTGRES_DB:-mydatabase}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      RAG_OPENAI_BASEURL: ${VLLM_BASE_URL}
      RAG_OPENAI_API_KEY: ${VLLM_API_KEY}
      EMBEDDINGS_PROVIDER: ${RAG_EMBEDDINGS_PROVIDER:-openai}
      EMBEDDINGS_MODEL: ${RAG_EMBEDDINGS_MODEL:?set RAG_EMBEDDINGS_MODEL in .env}
    networks: [backend]
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    mem_limit: 1g

  mcp-agent-skills:
    build:
      context: ./agent-skills
      dockerfile: mcp-server/Dockerfile
      args:
        TOOLSETS: ${MCP_TOOLSETS:-jira}
    restart: unless-stopped
    env_file: .env
    environment:
      # Honors X-Agent-Skills-Env-<VAR> per-request headers (e.g. a
      # different Jira credential per LibreChat user) instead of only this
      # server process's own env. Safe to enable *here* specifically
      # because this service has no published port and no Keycloak
      # provider configured -- `api` on the backend network is the only
      # caller that can reach it at all, which is the network-isolation
      # exception server.py's own startup warning calls out. See
      # agent-skills/AUTHENTICATION.md, Part 3.
      MCP_TRUST_REQUEST_CREDENTIALS: "1"
    networks: [backend]
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; socket.create_connection(('127.0.0.1', 8321), timeout=3)"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 15s
    mem_limit: 512m

  # The knowledge catalog's own database -- deliberately NOT another database
  # inside `vectordb`. An upgrade or restore of LibreChat's RAG store can't
  # touch the catalog, backups and retention are independent, and `kb` never
  # holds a credential to the database LibreChat's own data lives in
  # (docs/spec/knowledge-base.md, NFR-DEP-01). Schema and the least-privilege
  # query role come from `make kb-init`.
  kb-db:
    image: pgvector/pgvector:0.8.0-pg15-trixie
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${KB_POSTGRES_DB:-agentflow_kb}
      POSTGRES_USER: ${KB_POSTGRES_USER:-kb}
      POSTGRES_PASSWORD: ${KB_POSTGRES_PASSWORD:?set KB_POSTGRES_PASSWORD in .env}
    volumes:
      - kb_data:/var/lib/postgresql/data
    networks: [backend]
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${KB_POSTGRES_USER:-kb} -d ${KB_POSTGRES_DB:-agentflow_kb}"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s
    mem_limit: 512m

  # The knowledge catalog's MCP server: two read-only tools (kb_search, kb_get)
  # for the agents. Same isolation rule as mcp-agent-skills -- no published
  # port, backend network only, reachability from `api` is the access control.
  # Unlike that server it takes no credentials at all: the catalog is shared, so
  # there is nothing per-user for a caller to supply. It connects as kb_reader
  # (read-only, plus the gap log), never as the database owner.
  mcp-kb:
    build:
      context: ./kb
    restart: unless-stopped
    depends_on:
      kb-db:
        condition: service_healthy
    environment:
      KB_DATABASE_URL: postgresql://kb_reader:${KB_READER_PASSWORD:?set KB_READER_PASSWORD in .env}@kb-db:5432/${KB_POSTGRES_DB:-agentflow_kb}
      KB_MCP_HOST: 0.0.0.0
      KB_MCP_PORT: "8322"
    networks: [backend]
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; socket.create_connection(('127.0.0.1', 8322), timeout=3)"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 15s
    mem_limit: 512m

  # Operator commands for the catalog: sync, discovery, overrides, status.
  # Never started by `make up` -- it runs one command and exits (`make kb-sync`,
  # `make kb-sources-discover`). Separate from mcp-kb on purpose: writing needs
  # the database owner, while the query path holds the read-only role and must
  # keep holding only that.
  kb-cli:
    build:
      context: ./kb
    profiles: ["cli"]
    depends_on:
      kb-db:
        condition: service_healthy
    # Source credentials (a read-only service account for sync) and the
    # summarizer endpoint come from .env, same as every other service.
    env_file: .env
    environment:
      KB_DATABASE_URL: postgresql://${KB_POSTGRES_USER:-kb}:${KB_POSTGRES_PASSWORD:?set KB_POSTGRES_PASSWORD in .env}@kb-db:5432/${KB_POSTGRES_DB:-agentflow_kb}
      KB_SOURCES_FILE: /opt/kb/config/kb-sources.yaml
    # The summarizer calls the model endpoint, so this needs the same host pin
    # `api` and `rag_api` carry.
    extra_hosts:
      - "${MODEL_HOSTNAME:-your-model-host}:${MODEL_HOST_IP:-192.0.2.1}"
    volumes:
      # Writable: `make kb-sources-discover WRITE=1` appends candidates to it.
      - ./config/kb-sources.yaml:/opt/kb/config/kb-sources.yaml
    networks: [backend]
    entrypoint: ["python3", "-m", "kb"]

  # Runs sync on the cadences config/kb-sources.yaml gives, drains the lazy
  # refresh queue, and records every run. Its own service, not a thread inside
  # mcp-kb: a full scrape of a large space runs for minutes and a search must
  # never wait behind one. It is also the only component holding both source
  # credentials and a writable database role.
  kb-scheduler:
    build:
      context: ./kb
    restart: unless-stopped
    depends_on:
      kb-db:
        condition: service_healthy
    env_file: .env
    environment:
      KB_DATABASE_URL: postgresql://${KB_POSTGRES_USER:-kb}:${KB_POSTGRES_PASSWORD:?set KB_POSTGRES_PASSWORD in .env}@kb-db:5432/${KB_POSTGRES_DB:-agentflow_kb}
      KB_SOURCES_FILE: /opt/kb/config/kb-sources.yaml
    extra_hosts:
      - "${MODEL_HOSTNAME:-your-model-host}:${MODEL_HOST_IP:-192.0.2.1}"
    volumes:
      - ./config/kb-sources.yaml:/opt/kb/config/kb-sources.yaml:ro
    networks: [backend]
    command: ["python3", "-m", "kb.adapters.inbound.scheduler"]
    mem_limit: 512m

  # GitLab push events, for seconds-fresh updates instead of the 15-minute
  # tick. The one component that accepts an inbound connection, so: its own
  # service, bound to 127.0.0.1 (put a reverse proxy in front if GitLab lives
  # elsewhere), every request checked against KB_WEBHOOK_SECRET, and no work of
  # its own -- it queues paths the scheduler then refreshes. Disabled unless
  # you give it a secret; start it with `--profile webhook`.
  kb-webhook:
    build:
      context: ./kb
    profiles: ["webhook"]
    restart: unless-stopped
    depends_on:
      kb-db:
        condition: service_healthy
    env_file: .env
    environment:
      KB_DATABASE_URL: postgresql://${KB_POSTGRES_USER:-kb}:${KB_POSTGRES_PASSWORD:?set KB_POSTGRES_PASSWORD in .env}@kb-db:5432/${KB_POSTGRES_DB:-agentflow_kb}
      KB_SOURCES_FILE: /opt/kb/config/kb-sources.yaml
      KB_WEBHOOK_HOST: 0.0.0.0
      KB_WEBHOOK_PORT: "8323"
    volumes:
      - ./config/kb-sources.yaml:/opt/kb/config/kb-sources.yaml:ro
    ports:
      - "127.0.0.1:${KB_WEBHOOK_PORT:-8323}:8323"
    networks: [backend]
    command: ["python3", "-m", "kb.adapters.inbound.webhook"]
    mem_limit: 512m

  searxng:
    image: searxng/searxng:2026.9.8-3fdc6d753
    restart: unless-stopped
    volumes:
      - ./searxng/settings.yml:/etc/searxng/settings.yml:ro
    networks: [backend]
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; socket.create_connection(('127.0.0.1', 8080), timeout=3)"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 15s
    mem_limit: 512m
